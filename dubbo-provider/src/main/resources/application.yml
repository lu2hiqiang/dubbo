spring:
    application:
        name: dubbo-provider
server:
  port: 8001

dubbo:
  application:
    name: dubbo-provider
  protocol:
    name: dubbo
    port: 20881
  registry:
    address: zookeeper://127.0.0.1:2181
  scan:
    base-packages: 'com.dubbo.provider.service'